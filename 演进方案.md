# GoTryCatch 演进方案

> 基于对仓库全部源码（gotrycatch.go、errors/errors.go、约 3200 行测试、examples、cmd/demo）的逐行审阅。
> 基线：v1.3.0，`go test ./...` 全绿，`go vet` 无告警。
> 撰写日期：2026-08-15

---

## 目录

1. [现状盘点](#一现状盘点)
2. [问题清单](#二问题清单)
3. [演进路线总览](#三演进路线总览)
4. [阶段一 v1.4：正确性修复与工程化](#四阶段一-v14正确性修复与工程化零破坏)
5. [阶段二 v1.5：内部去重与结构收敛](#五阶段二-v15内部去重与结构收敛零破坏)
6. [阶段三 v2.0：语义升级](#六阶段三-v20语义升级含破坏性变更)
7. [明确不做的事](#七明确不做的事yagni)
8. [风险与回滚](#八风险与回滚)

---

## 一、现状盘点

### 1.1 模块结构

```
github.com/linkerlin/gotrycatch
├── gotrycatch.go          (425 行)  核心：Try / Catch / Finally 双体系
├── gotrycatch_test.go      (1856 行)
├── integration_test.go     (1150 行)
├── errors/
│   ├── errors.go           (673 行)  7 种预定义错误类型
│   └── errors_test.go      (721 行)
├── examples/main.go        (342 行)  10 个 Demo
├── cmd/demo/main.go        (126 行)  快速演示
└── go.mod                  (go 1.25)
```

两个生产模块之间**零依赖**（gotrycatch 不 import errors，反之亦然）——这是当前架构最健康的一点，演进中必须保持。

### 1.2 能力矩阵

| 能力 | 入口 | 形态 |
|------|------|------|
| 无返回值 try-catch | `Try` / `Catch[T]` / `CatchAny` / `Finally` | 函数 + 方法混合链式 |
| 带返回值 try-catch | `TryWithResult[T]` / `CatchWithResult[T,E]` / `CatchAnyWithResult` / `Finally` / `OnSuccess` / `OnError` / `OrElse` / `OrElseGet` | 函数 + 方法混合链式 |
| handler 返回值 | `CatchWithReturn[T]` | 函数，返回 `interface{}` |
| 断言 | `Assert` / `AssertNoError` / `Throw` | 函数 |
| 调试 | `SetDebug` / `IsDebug` | 全局开关 |
| 预定义错误 | errors 包 7 种类型 × (Error/Is/Unwrap/ToMap/ToJSON/New) | 值类型 + 构造函数 |

### 1.3 重复度体检（deletion test）

对每个可疑结构问：删掉它，复杂度是**集中**了还是只是**搬家**了？

| 结构 | 行数 | 份数 | 判定 |
|------|------|------|------|
| `TryBlock` 与 `TryBlockWithResult[T]` 的状态查询/Catch/Finally 逻辑 | ~180 | 2 | **搬家级重复**：两套类型各自维护 err/handled 字段及完全相同的守卫逻辑 |
| errors 包"基础字段 + captureCaller + captureStack + stackStrs 循环 + ToMap 模板" | ~70 | 7 | **搬家级重复**：新增一种错误类型需复制 70 行 |
| examples 与 cmd/demo | ~470 | 2 | 部分重复：职责有重叠，可收敛 |
| 测试的"字段非空"断言块 | ~15/类型 | 10+ | 适合表驱动收敛 |

---

## 二、问题清单

按严重程度分级：**A 正确性/语义 > B API 设计 > C 重复/可维护性 > D 工程化**。

### A 级：正确性与语义（P0）

#### A1. `Catch[T]` 不支持包装错误 —— 缺失 `errors.As` 语义

`gotrycatch.go:143` 用精确类型断言 `tb.err.(T)` 匹配。Go 1.13+ 的错误处理惯例是 `fmt.Errorf("%w", err)` 包装，本库无法穿透：

```go
// 用户代码：标准 Go 惯例包装底层错误
panic(fmt.Errorf("query failed: %w", errors.NewDatabaseError(...)))

// Catch[errors.DatabaseError] 永远匹配不上 —— panic 值是 *fmt.wrapError，
// 不是 DatabaseError。错误被静默漏到 CatchAny 或 Finally 重抛。
```

这是当前库与 Go 生态之间最大的语义鸿沟。库自称"类型安全异常处理"，却不能与 `errors.Is/As` 体系互认。

#### A2. 值/指针二义性

errors 包所有构造函数返回**值类型**（如 `ValidationError`），但 Go 惯例是 panic 指针（`panic(&err)`）或返回 `error` 接口。`Catch[ValidationError]` 匹配不了 `panic(&ValidationError{...})`，`Catch[error]` 反而能匹配一切实现了 `Error()` 的值。文档完全没有交代这个坑。

#### A3. 未处理错误静默吞没

`Try` 捕获 panic 后，如果调用方**既不 Catch 也不 Finally**，错误彻底消失——没有日志、没有重抛、没有任何提示。在后台任务里这等于把数据损坏藏起来。当前唯一防线是"用户记得写 Finally"，而 `Finally` 的重抛语义（未处理则 re-panic，gotrycatch.go:216-219）又只能在被调用时触发。

#### A4. handler 内 panic 会跳过 Finally

`Catch` 是普通函数调用而非 defer 结构：handler 里 panic 会立刻向上传播，后续的 `tb.Finally(...)` 根本没机会执行（测试 `TestFinally_CalledAfterCatchPanic` 确认了这个行为）。这与 Java/C#/Python 的 try-catch-finally 语义（finally 总会执行）相悖，而这恰是用户对本库的心智预期。

#### A5. `SetDebug` 数据竞争

`gotrycatch.go:35` 全局 `debugMode` 无原子保护。并发 goroutine 一边 `SetDebug` 一边走 `debugLog`，`go test -race` 会报 data race。现有测试没触发是因为没有并发开关调试。

#### A6. go.mod 与文档的版本承诺不一致

`go.mod` 声明 `go 1.25`，而 README/AGENTS.md 承诺"Go 1.18+（泛型支持所需）"。用 1.18–1.24 工具链的用户无法编译本模块。当前代码实际未用任何 1.18 之后的语言特性，声明 1.25 是误伤。

#### A7. `Finally` 内 panic 覆盖原错误（文档问题）

`gotrycatch.go:215` `defer fn()` 与重抛 `panic(tb.err)` 共存时，`fn` 自己 panic 会吞掉原始错误。这与 Go 原生 defer 语义一致，可接受，但需文档明示。

#### A8. goroutine 内 panic 不可捕获（文档问题）

`Try` 的 recover 只覆盖 `fn` 同 goroutine。`fn` 里 `go func(){ panic(...) }()` 会直接崩进程。需在 README 显著位置说明。

### B 级：API 设计（P1）

#### B1. `CatchWithReturn` 返回 `interface{}` —— 与库的类型安全主张自相矛盾

`gotrycatch.go:157`。`TryWithResult`/`CatchWithResult` 体系已完整覆盖该需求且类型安全。这个函数是更早的浅设计残留，测试在用但无真实价值。

#### B2. 包名 `errors` 与标准库冲突

用户必须写 `trycatcherrors "github.com/linkerlin/gotrycatch/errors"`（测试文件第 7 行就是这么做的）。Go 风格指南明确建议避免与标准库撞名。v2 是改名的唯一窗口。

#### B3. 两套体系 API 不对称 + 链式规则复杂

- `TryBlock` 没有 `OnError`/`OnSuccess` 对应物；`TryBlockWithResult` 没有 `GetErrorType`。
- `Catch`/`CatchWithResult`/`CatchAnyWithResult` 是函数，`CatchAny`/`Finally` 是方法（Go 泛型限制所迫，AGENTS.md 专门用一节解释）。用户需要记住"哪个能链、哪个要赋值"——**接口的规则几乎和实现一样复杂，这是典型的浅接口**。

#### B4. `GetErrorType()` 返回裸 `%T`

指针类型带 `*` 前缀（`*errors.ValidationError`）、接口动态类型各异。该 API 的目标用户是 Agent（AGENTS.md 明说），输出不归一化会增加判断成本。

#### B5. `OrElseGet(nil)` 直接 panic

`gotrycatch.go:420` 无 nil 守卫。库内其他所有入口都防御 nil，唯独这里不一致。

#### B6. `Try` 捕获时不记录堆栈

errors 包的错误自带创建时堆栈，但 `panic("string")` / `panic(err)` 这类普通 panic 被 `Try` 捕获后丢失现场。排障时只有错误值没有位置。

### C 级：重复与可维护性（P2）

#### C1. errors 包 7 类型复制粘贴（~490 行模板代码）

每个类型重复：5 个基础字段、`captureCaller(1)`、`captureStack(1)`、stack→string 转换循环、ToMap 的 7 个固定键、Unwrap 返回 nil 的死方法。AGENTS.md 的"扩展错误类型步骤"要求 7 步、约 70 行——加第 8 种错误的边际成本过高，说明抽象缺席。

#### C2. 主包两套 Try/Catch/Finally 完全平行（~180 行重复）

`Catch` 与 `CatchWithResult`、`CatchAny` 与 `CatchAnyWithResult`、两个 `Finally` 的守卫与分发逻辑逐行雷同。修一个 bug 要改两处（A1 的修复若不做 C2，工作量直接翻倍——这是本项的杠杆所在）。

#### C3. examples/ 与 cmd/demo/ 职责重叠

两个可执行入口都演示同样的 API。维护双份演示意味着每个新特性要写两遍示例。

#### C4. 测试质量瑕疵

- `gotrycatch_test.go:1586`：`if r := recover(); r != r` —— 恒假断言，等于没验证。
- 3200 行测试大量"字段非空"重复块，适合表驱动收敛。

### D 级：工程化（P3）

| # | 问题 | 说明 |
|---|------|------|
| D1 | 无 CI | 没有 GitHub Actions，测试/竞态/vet 不自动跑 |
| D2 | 无 lint 配置 | 无 golangci-lint、无 gofmt 检查 |
| D3 | 无基准测试 | recover 封装的开销从未量化，"能不能用于热路径"无答案 |
| D4 | `Version` 硬编码 | 发布依赖手工同步常量与 tag（低优先级） |

---

## 三、演进路线总览

原则：**每一步独立可发布、可回滚；破坏性变更只集中在 v2.0 一次**。

| 阶段 | 版本 | 主题 | 破坏性 | 核心交付 |
|------|------|------|--------|----------|
| 一 | v1.4 | 正确性 + 工程化 | 无 | A5/A6 修复，A7/A8 文档，D1–D3，B5，C4 假断言修复 |
| 二 | v1.5 | 内部去重 | 无（行为兼容） | C1 BaseError 嵌入、C2 双体系内部统一、C3 演示收敛 |
| 三 | v2.0 | 语义升级 | 有（受控） | A1 CatchAs、A3/A4 Run 子句式 API、B1 删除、B2 包改名（带兼容 alias） |

依赖关系：**A1 的修复强烈建议在 C2 之后做**（只改一处分发核心）；B2 的 alias 转发依赖 C1 完成。

---

## 四、阶段一 v1.4：正确性修复与工程化（零破坏）

### 4.1 修复 `SetDebug` 数据竞争（A5）

```go
// go 1.18 兼容写法（不用 atomic.Bool，那是 1.19+）
var debugMode int32 // 0 = off, 1 = on

func SetDebug(enabled bool) {
	v := int32(0)
	if enabled { v = 1 }
	atomic.StoreInt32(&debugMode, v)
}

func debugLog(format string, args ...interface{}) {
	if atomic.LoadInt32(&debugMode) == 1 {
		debugLogger.Printf(format, args...)
	}
}
```

验收：新增并发测试（10 goroutine 交替 SetDebug(true/false) + Try），`go test -race` 通过。

### 4.2 统一 Go 版本承诺（A6）✅ 已执行，决策修正

方案原文写"降为 go 1.18"，执行时发现该承诺不诚实：`TestNilPanicValue` 及文档声称的
"panic(nil) 也算 HasError"依赖 Go 1.21+ 的 `panic(nil)` 运行时语义（1.21 起 recover 返回
`*runtime.PanicNilError`；go.mod 声明 <1.21 时 GODEBUG=panicnil 回退旧行为，recover 返回 nil，
库行为随环境漂移）。**实际落地：go.mod 定为 `go 1.21`，README 徽章/双语兼容性/AGENTS.md 同步。**
若阶段三引入 `errors.Join` 等 1.20+ 特性，届时再按需上调并同步文档。

### 4.3 `OrElseGet` nil 守卫（B5）

```go
func (tb *TryBlockWithResult[T]) OrElseGet(supplier func() T) T {
	if tb == nil || tb.err != nil {
		if supplier == nil {
			var zero T
			return zero
		}
		return supplier()
	}
	return tb.result
}
```

### 4.4 修复假断言（C4 部分）

`gotrycatch_test.go:1586` 的 `r != r` 改为 `r != "unhandled"`。

### 4.5 文档补齐语义边界（A7、A8、A2 的文档面）

README 增加"语义边界"一节，四条：

1. handler 内 panic 会向上传播且跳过后续 Finally（链接到 v2.0 的 Run API 作为解法）。
2. Finally 内 panic 会覆盖原始错误（与 Go 原生 defer 一致）。
3. `Try` 不捕获 `fn` 内启动的 goroutine 的 panic。
4. `Catch[T]` 是精确类型断言：`panic(&err)` 不匹配 `Catch[Err]`，值/指针需对齐；包装错误的穿透见 v2.0 `CatchAs`。

### 4.6 CI（D1）

`.github/workflows/ci.yml`，三个 job：

```yaml
test:    go test -race ./...
vet:     go vet ./...
lint:    golangci-lint run   # 使用 golangci-lint-action v6
matrix:  go 1.18.x / 1.21.x / 1.25.x 三档（同时守护版本承诺）
```

### 4.7 基准测试（D3）

`gotrycatch_bench_test.go`：

```go
func BenchmarkTryNoPanic(b *testing.B)     // 快乐路径
func BenchmarkTryWithPanic(b *testing.B)   // panic + Catch 命中
func BenchmarkTryWithResult(b *testing.B)
func BenchmarkRawRecover(b *testing.B)     // 原生 recover 基线
func BenchmarkErrorReturn(b *testing.B)    // error 返回值基线
```

产出数字写入 README（"性能"一节），回答"能否用于热路径"。

**发布检查**：全部测试 `-race` 通过；1.18/1.21/1.25 三档矩阵绿；基准数字入档。

---

## 五、阶段二 v1.5：内部去重与结构收敛（零破坏）✅ 已执行（2026-08-15）

> 执行结果：5.1–5.4 全部落地。门禁达成——现有约 3400 行测试零修改全绿（-race）；
> 堆栈归因与原版经 git worktree 实测逐项一致（File/Line/Stack[0]/栈深）；
> 基准无回退反而优化（Try+panic+Catch 496→417 ns/op，mismatch 调试日志改为惰性求值，
> 分配数不变，仍是 1 alloc）。errors.go 673→443 行，gotrycatch.go 425→394 行。
> 5.3 采用"整体并入"路线：examples/ 10 个 Demo（cmd/demo 的超集）并入 cmd/demo 后删除
> examples/，README/AGENTS/USAGE/教程 引用同步。

### 5.1 主包：统一分发核心（C2）

不动公开 API，只收内部实现。`TryBlockWithResult[T]` 持有共享的核心：

```go
// blockCore 承载全部 err/handled 状态与分发逻辑（包内私有）。
type blockCore struct {
	err     interface{}
	handled bool
}

func (c *blockCore) dispatch(match func(interface{}) bool, run func(interface{})) bool {
	if c.err != nil && !c.handled && match(c.err) {
		run(c.err)
		c.handled = true
		return true
	}
	return false
}
```

`TryBlock` 与 `TryBlockWithResult[T]` 各自的字段改为嵌入 `blockCore`，`Catch`/`CatchWithResult`/`CatchAny`/`CatchAnyWithResult` 全部委托 `dispatch`。两套 ~180 行守卫逻辑收敛为一份。

**这一步是 A1 的前置**：阶段三只需给 `blockCore.dispatch` 增加 As 链匹配一种能力，全家族（Catch/CatchWithResult/CatchAnyWithResult）同时受益。

验收：现有 3200 行测试零修改全绿（证明行为兼容）；代码行数净减。

### 5.2 errors 包：BaseError 嵌入（C1）

```go
// BaseError 携带所有错误类型共有的定位与堆栈信息。
type BaseError struct {
	File      string    `json:"file"`
	Line      int       `json:"line"`
	Function  string    `json:"function"`
	Timestamp time.Time `json:"timestamp"`
	Stack     []string  `json:"stack"`
}

func newBase(skip int) BaseError {
	file, line, fn := captureCaller(skip + 1)
	stack := captureStack(skip + 1)
	strs := make([]string, len(stack))
	for i, s := range stack {
		strs[i] = fmt.Sprintf("%s:%d %s", s.File, s.Line, s.Func)
	}
	return BaseError{file, line, fn, time.Now(), strs}
}

// baseToMap 输出所有类型共有的 7 个键。
func baseToMap(e BaseError) map[string]interface{} { ... }
```

各类型变为专有字段 + 嵌入：

```go
type ValidationError struct {
	BaseError
	Field  string `json:"field"`
	Message string `json:"message"`
	Code   int    `json:"code"`
}

func NewValidationError(field, message string, code int) ValidationError {
	return ValidationError{BaseError: newBase(1), Field: field, Message: message, Code: code}
}
```

**兼容性**：嵌入字段是可寻址的，`err.File` / `err.Stack` 等字段访问语法不变；`ToMap()` 输出的键完全不变。唯一的可见差异是 `fmt.Sprintf("%+v", err)` 的输出结构——风险极低，在 release note 说明即可。**共删除约 350 行模板**，"扩展错误类型"从 7 步 70 行降到 2 步 15 行。

### 5.3 演示入口收敛（C3）

`cmd/demo` 保留（`go run ./cmd/demo` 是 AGENTS.md 记载的入口），`examples/` 的 10 个 Demo 中与 demo 重复的部分删除，只保留 cmd/demo 覆盖不到的场景（或整体并入 cmd/demo 的子命令）。一个演示入口，一份维护成本。

### 5.4 测试表驱动化（C4）

错误类型测试收敛为表驱动：一张 `[]struct{ name string; new func() ErrorLike; check func(t) }`，7 类型 × 3 重复断言块变一张表。行数预计减半，新增类型的测试从"复制 60 行"变"加一行表项"。

**发布检查**：公开 API 的 godiff 为空（`go doc` 输出对比）；测试全绿且行数下降。

---

## 六、阶段三 v2.0：语义升级（含破坏性变更）✅ 已执行（2026-08-15）

> 执行结果：6.1–6.5 全部落地。Run/Run1 + On/OnAs/Any/Cleanup 子句 API、CatchAs 家族、
> Err() 桥接、CanonicalErrorType 均在 run.go（新增）；CatchWithReturn 已删除（测试同步清理）；
> errtypes 正名 + errors/ 纯别名层——**执行改进**：构造函数用 `var NewXxx = errtypes.NewXxx`
> 直接绑定而非包装函数，转发零中间帧，File/Line/Stack 归因与直连 errtypes 完全一致
> （实测验证）；forward_test.go 验证别名同一性。28 个新测试（含 %w 三层穿透、Cleanup 三态、
> handler panic 后清理仍执行、error 直通的 errors.Is 链、并发）。Version 2.0.0，
> README 双语 v2 章节 + 迁移指南。

### 6.1 `CatchAs`：errors.As 语义的类型穿透（A1、A2）

v1.5 的 `blockCore.dispatch` 之上，新增一族匹配器。**不改 `Catch` 的既有语义**（精确断言），新增显式函数：

```go
// CatchAs 在错误链上按 errors.As 语义匹配 E。
// panic(fmt.Errorf("wrap: %w", dbErr)) 也能被 CatchAs[DatabaseError] 命中。
func CatchAs[E error](tb *TryBlock, handler func(E)) *TryBlock

// 同时覆盖值/指针二义性：E 可以是 *DatabaseError 或 DatabaseError。
func CatchAsWithResult[T any, E error](tb *TryBlockWithResult[T], handler func(E)) *TryBlockWithResult[T]
```

实现核心（一处修改，全家族受益——5.1 的杠杆兑现）：

```go
func asMatch[E error](v interface{}) (E, bool) {
	var target E
	if err, ok := v.(error); ok && errors.As(err, &target) {
		return target, true
	}
	// panic 值本身不是 error 时，也允许直接断言（兼容 panic(DatabaseError{...})）
	t, ok := v.(E)
	return t, ok
}
```

为什么不悄悄改 `Catch`：`Catch[error]` 现在是"任何 error"，若变成 As 穿透，行为变化无法静态察觉。**新语义给新名字**，旧代码零惊吓。

验收：包装错误三层嵌套穿透测试；值/指针双形态测试；`Catch` 旧语义回归测试全部不变。

### 6.2 `Run`：子句式块级 API（A3、A4 —— 本方案的最高杠杆项）

#### 动机（用深化标准衡量现状）

当前接口要求用户记住五条规则：必须 `Finally` 否则吞错、`Catch` 是函数 `CatchAny` 是方法、子句顺序敏感、handler panic 跳过 Finally、未处理错误在 Finally 里重抛。**接口的规则集合 ≈ 实现的复杂度**——浅接口。删除测试：把这些规则全删掉、收进一个函数里，复杂度**集中**而非搬家——深化的明确信号。

#### 方案

```go
type Clause struct {
	match   func(interface{}) bool   // nil = always (Any / Cleanup)
	run     func(interface{})
	cleanup bool                     // Cleanup 子句
}

// 子句构造器（泛型发生在构造时，Run 本身无类型参数）
func On[E any](h func(E)) Clause        // 精确类型匹配（现 Catch 语义）
func OnAs[E error](h func(E)) Clause    // errors.As 语义（6.1）
func Any(h func(interface{})) Clause    // 兜底
func Cleanup(fn func()) Clause          // 清理，总是执行

// Run 执行 fn，按声明顺序分发 panic，返回未处理的错误值。
// 保证：Cleanup 总是执行（defer 注册）；handler panic 不会跳过后续 Cleanup；
// 未处理错误显式返回，不再依赖调用方记得 Finally。
func Run(fn func(), clauses ...Clause) (unhandled interface{})
```

用户视角：

```go
if unhandled := gotrycatch.Run(queryUser,
	gotrycatch.OnAs(func(e *errtypes.DatabaseError) { retry(e) }),
	gotrycatch.On(func(e errtypes.RateLimitError) { wait(e.RetryAfter) }),
	gotrycatch.Cleanup(func() { conn.Close() }),
); unhandled != nil {
	log.Fatalf("unhandled: %v", unhandled)  // 吞错在结构上不可能发生
}
```

实现骨架：

```go
func Run(fn func(), clauses ...Clause) (unhandled interface{}) {
	var cleanups []func()
	defer func() {
		if r := recover(); r != nil {
			unhandled = r
			for _, c := range clauses {          // 分发
				if c.cleanup { continue }
				if unhandled == nil { break }
				if c.match == nil || c.match(unhandled) {
					c.run(unhandled)
					unhandled = nil              // 已处理
				}
			}
		}
		for i := len(cleanups) - 1; i >= 0; i-- { // Cleanup 最后、逆序、总是执行
			cleanups[i]()
		}
	}()
	for _, c := range clauses {                   // 先登记清理
		if c.cleanup && c.run != nil { cleanups = append(cleanups, c.run2) }
	}
	fn()
	return nil
}
```

（示意；handler 内 panic 的链接与吞没策略在实现时定夺——初版可让 handler panic 直接传播并在文档标注，与 v1 行为对齐。）

#### 语义对比

| 场景 | v1 链式 | v2 Run |
|------|---------|--------|
| 忘记 Finally | 错误静默吞没 | 结构上不可能（未处理必返回） |
| handler panic | 跳过 Finally | Cleanup 仍执行（defer 保证） |
| 子句形态 | 函数/方法混合，规则要背 | 全部是值子句，顺序即优先级 |
| goroutine 内使用 | 每条链手工传 | `go func(){ Run(...) }()` 自然成立 |

**v1 API 全部保留**（`Try`/`Catch`/... 不删），Run 是增量入口。测试面：Run 全场景表驱动 + 竞态 + 基准（子句构造的分配开销 vs 链式调用）。

### 6.3 删除 `CatchWithReturn`（B1）

v2.0 唯一的删除项。功能被 `TryWithResult` + `CatchWithResult` 完全覆盖且后者类型安全。迁移指南一行：

```go
// before
result, tb := CatchWithReturn(tb, func(err string) interface{} { ... })
// after
tb = TryWithResult(func() T { ... })  // 在函数体内返回值
tb = CatchWithResult[T, string](tb, func(err string) { ... })
result := tb.OrElse(defaultValue)
```

### 6.4 errors 包改名 + 兼容转发（B2，依赖 5.2）

新路径 `github.com/linkerlin/gotrycatch/errtypes`。旧路径 `errors/errors.go` 变为纯转发文件（14 行），**编译兼容，零迁移成本**：

```go
package errors

import "github.com/linkerlin/gotrycatch/errtypes"

type ValidationError = errtypes.ValidationError
func NewValidationError(f, m string, c int) ValidationError { return errtypes.NewValidationError(f, m, c) }
// ... 7 类型 + 8 构造函数同构转发
```

release note 标注旧路径冻结新增（只在新包加类型），两个大版本后移除。

### 6.5 `GetErrorType` 归一化（B4）

新增归一化形式，旧方法不动：

```go
// CanonicalErrorType 返回去指针、去泛型参数的短类型名，如 "ValidationError"。
func (tb *TryBlock) CanonicalErrorType() string
```

实现：`reflect.TypeOf(tb.err)`，循环 `Elem()` 剥指针，`Name()` 取短名。面向 Agent 消费。

### v2.0 发布门禁

- v1 全 API 回归测试绿（除 `CatchWithReturn` 显式移除）
- `Run` / `CatchAs` / `OnAs` 各自独立的表驱动测试 + `-race`
- 基准报告更新（Run vs 链式 vs 原生 recover）
- README 双语 + 教程.md + AGENTS.md 同步（AGENTS.md 的"方法链式调用规则"一节重写——Run 体系下规则消失）
- semver: v2.0.0，go.mod 保持 1.18（除非确需新特性）

---

## 七、明确不做的事（YAGNI）

| 提议 | 不做的理由 |
|------|-----------|
| `tc.Catch[T]` 方法形式 | Go 语言限制（方法不能有类型参数），Run 的子句构造器已绕开 |
| panic 值自动装箱为 error / 强制 error 化 | 违背库"任意值 panic 皆可捕获"的立身之本 |
| `Runtime.SetFinalizer` 检测吞错的 TryBlock | 魔法、不可靠、GC 依赖；Run 已结构性解决 |
| 内置重试/超时/熔断子句 | 与错误处理正交，用户一行 `OnAs` + 自己的循环即可组合 |
| errors 包继续加类型（PaymentError 等） | 领域无边界，7 个已够 demo 场景；v1.5 后用户自造类型成本 15 行 |
| 结构化日志钩子 / OpenTelemetry 集成 | 无真实需求信号；debugLog 已可覆盖排查 |
| context 传递子句 | fn 闭包自然捕获 ctx，无需框架介入 |

---

## 八、风险与回滚

| 变更 | 风险 | 缓解 | 回滚 |
|------|------|------|------|
| 4.1 atomic 改造 | 极低 | 纯内部 | revert 单 commit |
| 5.1 blockCore | 行为等价性证明靠测试 | 3200 行存量测试零修改全绿为门禁 | 独立 commit revert |
| 5.2 BaseError 嵌入 | `%+v` 输出结构变化 | release note 明示；ToMap/JSON 键不变 | 独立 commit revert |
| 6.1 CatchAs 新语义混入 Catch | 用户预期错乱 | 已决策：新语义只给新名字 | N/A（不动旧的） |
| 6.2 Run | handler panic 链式策略复杂 | 初版对齐 v1 行为（传播），文档标注；复杂化留给 v2.1 | Run 是增量 API，可整体删除 |
| 6.4 包改名 | import 路径断裂 | 兼容转发文件保编译；文档双路径 | 转发文件永久保留也可接受（14 行） |

### 里程碑与工作量估算

| 阶段 | 内容 | 估算 |
|------|------|------|
| v1.4 | A5/A6/B5/C4 部分/文档/CI/基准 | 1–2 天 |
| v1.5 | C1/C2 重构 + 测试表驱动 + C3 | 2–3 天 |
| v2.0 | CatchAs + Run + 删除/改名/归一化 + 全文档 | 3–5 天 |

合计 6–10 个工作日，三个独立发布点，任意一步可停可回滚。

---

## 附：审阅覆盖清单

- [x] gotrycatch.go（425 行逐行）
- [x] errors/errors.go（673 行逐行）
- [x] gotrycatch_test.go（1856 行，发现 1 处假断言）
- [x] TODO.md（v1.3 前四阶段已完成项与本方案不冲突，本方案为其续篇）
- [x] go.mod / git log / 代码规模统计
- [ ] integration_test.go、errors_test.go、examples、cmd/demo（抽样；如需逐行审阅再补）
